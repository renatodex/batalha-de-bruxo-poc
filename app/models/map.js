var map = function(width, height, tile_height, tile_width, sprite, tile_data ){

	//function getY(tile_post) { return parseInt((tile_post-1)/tiles_per_line) * tile_width }
	//function getX(tile_pos) { ((tile_pos-1)%tiles_per_line)*tile_width }

	return{

	}
}